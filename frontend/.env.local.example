# Backend API Configuration
# Update this URL to point to your FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL (defaults to same as API_URL but with ws:// protocol)
# Uncomment and set if your WebSocket server is on a different URL
# NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Environment (development, staging, production)
NEXT_PUBLIC_ENV=development

# CSRF Protection - Allowed Origins (comma-separated)
# Add all origins that should be allowed to make requests to your API routes
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting (Upstash Redis) - OPTIONAL
# Uncomment to enable rate limiting on API routes
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Monitoring/Observability - OPTIONAL
# LangSmith integration for frontend tracing
# NEXT_PUBLIC_LANGSMITH_API_KEY=
# NEXT_PUBLIC_LANGSMITH_PROJECT=deep-agent-agi-frontend

# Error Tracking (e.g., Sentry) - OPTIONAL
# NEXT_PUBLIC_SENTRY_DSN=

# Feature Flags - OPTIONAL
# NEXT_PUBLIC_ENABLE_HITL=true
# NEXT_PUBLIC_ENABLE_STREAMING=true

# Agent Configuration - OPTIONAL
# NEXT_PUBLIC_AGENT_NAME=deepAgent
